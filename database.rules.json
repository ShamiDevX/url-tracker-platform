{
  "rules": {
    "locations": {
      "$deviceId": {
        ".read": true,
        ".write": "!data.exists() || data.child('deviceId').val() === $deviceId || !newData.exists()",
        ".validate": "newData.hasChildren(['deviceId']) && newData.child('deviceId').isString() && newData.child('deviceId').val().length <= 128 && (!newData.hasChild('latitude') || (newData.child('latitude').isNumber() && newData.child('latitude').val() >= -90 && newData.child('latitude').val() <= 90)) && (!newData.hasChild('longitude') || (newData.child('longitude').isNumber() && newData.child('longitude').val() >= -180 && newData.child('longitude').val() <= 180)) && (!newData.hasChild('userAgent') || (newData.child('userAgent').isString() && newData.child('userAgent').val().length <= 1000)) && (!newData.hasChild('referrer') || (newData.child('referrer').isString() && newData.child('referrer').val().length <= 1000))"
      }
    },
    "shareLinks": {
      ".read": true,
      ".write": true
    },
    "shortUrls": {
      ".read": true,
      ".write": true
    }
  }
}
